# Default checkpoint location for `play-interactive`.
#
# Drop a trained skrl checkpoint here as `policy.pt` to make it the
# default loaded by `uv run play-interactive`. Otherwise pass
# `--checkpoint <local-or-wandb>` on the CLI.
